import { PlaylistImgConverterPipe } from './playlist-img-converter.pipe';

describe('PlaylistImgConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaylistImgConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
