import { LyricsConverterPipe } from './lyrics-converter.pipe';

describe('LyricsConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new LyricsConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
