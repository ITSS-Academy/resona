import { ImgConverterPipe } from './img-converter.pipe';

describe('ImgConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
